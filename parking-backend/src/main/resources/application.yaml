spring:
  application:
    name: parking-backend

  devtools:
    restart:
      enabled: false

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/parking_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:12345}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
    defer-datasource-initialization: true

  sql:
    init:
      mode: ${SPRING_SQL_INIT_MODE:never}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:lephu2005sanh@gmail.com}
    password: ${MAIL_PASSWORD:tevwuohnjkryuzpq}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8080

app:
  frontend-base-url: ${APP_FRONTEND_BASE_URL:http://localhost:5173}

jwt:
  secret: ${JWT_SECRET:swp391-parking-dev-secret-key-must-be-at-least-256-bits-long-string}
  expiration: ${JWT_EXPIRATION:86400000}

google:
  client-id: ${GOOGLE_CLIENT_ID:1035279450306-nv8sjrtdev50chrsmrsetmin25o516he.apps.googleusercontent.com}

ocr:
  confidence-threshold: 0.85
  upload-dir: uploads/ocr
  engine-url: ${OCR_ENGINE_URL:http://localhost:8000/recognize}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

vnpay:
  tmn-code: YOMWKDME
  hash-secret: 4EQYIP7REP7K5TKBFLOMQAZFYRSF7P06
  pay-url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  return-url: ${VNPAY_RETURN_URL:http://localhost:5173/payment/result}
  ipn-url: ${VNPAY_IPN_URL:https://until-deeply-able.ngrok-free.dev/api/v1/payments/vnpay/ipn}
